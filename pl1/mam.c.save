#include<stdio.h>
 int main()
 {      char ar[12]="Green",br[12]="Blue";
	 int i,j,flag=0,len=0;
 	for(i=0;ar[i]!='\0';i++){  
 	len++; 
 	}
 	printf("len=%d\n",len);
 	i=0;
    for(j=0;br[j]!='\0';j++)
 	{  if(ar[i]==br[j])
 	{  flag++;
        i++;
        
    }
    else{   
    break;
	 }  
	 }
    if (flag==len)
    printf("equal");
    else
    printf("not equal");    }
