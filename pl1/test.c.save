#include<stdio.h>
#include<string.h>
int main(){
int i,j, len1, len2;
char ar[20];
char br[10];
gets(ar);
gets(br);
len1 = strlen(ar);
len2 = strlen(br);

for(i=len1, j=0; i<len1+len2; i++, j++){
    ar[i] = br[j];
    }
    br
printf("%s", ar);

}
